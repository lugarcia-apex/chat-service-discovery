apiVersion: v1
kind: Service
metadata:
  name: chat-service-discovery
spec:
  selector:
    app: chat-service-discovery
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8083
      nodePort: 30002
  type: NodePort
